extends layout

block content
    .main.container
        .row 
            .col-md-6 
            h1.display-4.m-b-5 #{title}

            div.form-group
                a.btn.btn-primary(href="/auth/facebook") Facebook
            div.form-group
                a.btn.btn-danger(href="/auth/google") Google